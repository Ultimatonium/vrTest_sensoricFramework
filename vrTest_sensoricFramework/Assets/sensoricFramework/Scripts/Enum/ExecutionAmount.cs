namespace SensoricFramework
{
    /// <summary>
    /// enum which defines how often something happens
    /// </summary>
    public enum ExecutionAmount
    {
        NONE,
        Once,
        Ongoing
    }
}
